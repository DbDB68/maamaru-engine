# 刀剑乱舞 MAA 脚本 · 开发交接文档

> 写给下一个接手优化/加新活动的 AI（也包括未来的 k3）。
> 主人（用户）非技术背景但游戏理解极深，说话直接，翻车了要直说，不许装成功。
> 本文最后更新：2026-07-30（南瓜大作战/刮刮乐智能版跑通之日）

---

## 0. 这个项目是什么

用 MAA Framework 的 Python 绑定（asst 模块）自动控制 MuMu 模拟器玩刀剑乱舞 -ONLINE-（日服）。
日常全托管：日课一条龙、联队战、刮刮乐、出阵、远征、内番、演练、手入、锻刀、刀解、合成、刷花。
带一个网页面板（手机也能用）、Agent 网关（Telegram/QQ Bot 遥控）、像素风仪表盘。

**一切游戏内容都在配置文件里，代码里不许硬编码任何游戏特定内容**（坐标、模板名、OCR 文本都进 `touken_config.json`）。这是架构红线。

---

## 1. 硬前提（动了就全崩，先检查这些）

| 项 | 值 | 为什么 |
|---|---|---|
| 模拟器分辨率 | **1280×720** | 所有模板图和坐标都按这个截的 |
| MuMu 显示缩放 | **100%** | 用户特意改过来的，截图才不偏 |
| ADB | `D:\MUMU\MuMuPlayer\nx_device\12.0\shell\adb.exe`，`127.0.0.1:16384` | 配置在 touken_config.json 顶层 |
| Python | **一律用 `./.venv/Scripts/python.exe`**，cwd = `W:\Maamaru` | 包都装在 venv 里 |
| 资源目录 | `resource/base` | MAA 的 bundle，模板图在 `resource/base/image/` 下 |

手动截图命令（调试用）：
```bash
"D:\MUMU\MuMuPlayer\nx_device\12.0\shell\adb.exe" -s 127.0.0.1:16384 exec-out screencap -p > x.png
```

---

## 2. 目录结构地图

```
W:\Maamaru\
├── touken_config.json      ← 全部游戏内容配置（1300+行，唯一事实源）
├── touken/                 ← 脚本本体
│   ├── maa_adapter.py      ← 底层：截图/点击/OCR/模板匹配（MAA 封装）
│   ├── navigator.py        ← 中层：界面识别 + 目录导航 + 弹窗处理
│   ├── agent.py            ← 组装：ToukenAgent = 所有 Mixin 的并集
│   ├── silhouette.py       ← 剪影识别（刮刮乐认刀，见 §8）
│   ├── sword_db.py         ← 刀剑数据（刀种/极化判定等）
│   ├── emulator.py         ← 模拟器启停（MuMuNxMain 管家）
│   ├── notify.py           ← 推送
│   └── flows/              ← 上层业务：一个文件一个玩法
│       ├── daily.py        ← 日课总编排（最大最复杂的文件）
│       ├── raid.py         ← 联队战（battle_loop_stream 通用战斗循环在这）
│       ├── pumpkin.py      ← 刮刮乐（死板+智能认人）
│       ├── battle/expedition/practice/repair/sakura/sortie/... ← 各玩法
│       └── __init__.py     ← 加新玩法要在这里注册 Mixin
├── panel/                  ← 网页面板（FastAPI + 原生JS）
│   ├── server.py           ← register_script 注册脚本入口 + 仪表盘文案
│   ├── script_runner.py    ← 后台跑脚本，日志三路走
│   ├── agent.py            ← Agent 网关（9 个工具）
│   ├── bot_qq.py / bot_telegram.py
│   └── static/             ← 前端（index.html/app.js/style.css）
├── resource/base/image/    ← 模板图库（按子目录分类：team/ battle/ menu/ 刮刮乐/ 刀种/...）
├── silhouette_lib/         ← 剪影素材库（231 个 PNG，群友做的，见 §8）
├── status/progress.json    ← 仪表盘进度横幅的数据源（set_progress 写）
└── test_*.py               ← 每个玩法一个实测入口（见 §10）
```

---

## 3. 架构三层，别越层写代码

```
flows/（玩法逻辑：点哪、什么时候点、翻车了怎么办）
  ↓ 只用
navigator.py（"我在哪/怎么去某界面"，navigate_to_stream）
  ↓ 只用
maa_adapter.py（screenshot/click/ocr/template_match，不知道自己在玩什么游戏）
```

- flows 里**不许直接调 asst**，一律走 `self.maa.xxx`。
- 流式汇报是约定：玩法函数写成生成器 `xxx_stream()`，`yield "[玩法] 人话日志"`，面板和终端都靠这个显示。
- 结果传递用 self 属性（如 `self._battle_started`、`self._refresh_ok`），这是既有风格，跟着写。

---

## 4. 模板图规范（新活动要截新图，照这个来）

1. **必须从那台模拟器实屏截**（1280×720），用 adb screencap 或游戏内实测时 `maa.save_screenshot()`。不能用网上找的图、不能缩放。
2. 裁模板时**只裁稳定的局部**：一个按钮就裁按钮本身，别带背景（背景会随活动换皮）。
3. 放到 `resource/base/image/` 合适的子目录，配置里写相对路径（如 `"刮刮乐/ui南瓜.png"`）。
4. 命名：`uiXXX.png` = 界面标题/标志物（用来确认"我在这个界面"）；动作按钮直接叫名字（`剪影更新.png`）。
5. ⚠️ **RGBA PNG（带透明通道）会让 MAA 的 ROI 模板匹配失灵**——`ui南瓜.png` 就是 RGBA，所以它的 `round_end` 配置**不能配 roi，只能全屏 + threshold 0.9**。新截图导出时注意用 RGB，不透明底就先转一下。

---

## 5. 识别手段怎么选

| 场景 | 用啥 |
|---|---|
| 确认"我在哪个界面" | 模板匹配界面标志物（`uiXXX.png`，threshold 0.7；全屏大图特征多的可用 0.9） |
| 按钮长得一样但位置不同（通用_确定/通用_取消） | 模板匹配 **+ roi 限定区域**（`roi_4to4(x0,y0,x1,y1)`） |
| 文字内容本身（"已解锁"/"部队选择"/数字） | OCR，**必须配 roi 缩小范围**，否则误读率爆炸 |
| 像素级状态（剪影、疲劳值、角标） | 专用模块（silhouette.py 那种），先标定再上线 |

**OCR 出来的字经常是碎的**（"战斗"能读成 "H"/"破"），所以 OCR 只做"命中/未命中"判断，别拿 OCR 文本做复杂逻辑。

---

## 6. 通用安全机制（所有新流程都必须继承的规矩）

1. **弹窗扫地**：登录后/回界面后永远不假设界面干净，先 `_ensure_on_board` 式确认（认标题 → 没有就点安全区 `skip_tap` 扒拉弹窗，8 次还不行就放弃上报）。
2. **冷启动担保**：`daily.py:_ensure_game_started`——点桌面图标前必须 OCR 验证图标下面写着「刀剑乱舞」四个字。**这是血泪教训**：没担保时模板撞脸模拟器广告，自动下载了别的游戏。
3. **完成判定靠官方文字，不靠猜**：刮刮乐板子满没满，看左侧「已解锁所有面板」OCR。猜错的代价是误点剪影更新白烧一枚令牌（真烧过）。
4. **拒答文化**：识别拿不准就明确说"认不准，继续打"，绝不硬猜。剪影识别连续两次认出同一个名字才动手（`pending_name` 复核机制，pumpkin.py §3.25）。
5. **一切花钱/烧资源的操作都要有上限**：`max_buys`（手形）、`max_skips`（更新令牌），到上限收工上报。
6. **防死循环**：所有 while 都有 `misses`/轮次上限，超过就停下喊人，不许无限点。
7. **刀剑破坏是绝对红线**：活动图不存在刀剑破坏可以自动；普通图出阵涉及重伤/手入判断，没把握就停下来叫人。
8. **刀装未满警告**：遇到 `equip_warning_button` 停下上报，不替用户做决定。

---

## 7. 战斗循环（raid.py 的 battle_loop_stream，已被联队战/刮刮乐复用）

```python
for msg in self.battle_loop_stream(cfg_key="pumpkin", tag="[南瓜]",
                                   need_battle=False, debug_dir=None):
    yield msg
round_done, _ = self._battle_loop_result
```

- `need_battle=True`：要 OCR 到"战斗"按钮并连点（联队战这种半自动）。
- `need_battle=False`：全自动战斗（刮刮乐），靠冷静期 + 界面标题回归判结束。
- 结束判定：`round_end` 配置（模板或 OCR）重新出现 = 回到出发界面。
- 有安全上限，`round_done=False` 就是超时了，流程必须停下喊人。

---

## 8. 剪影识别模块（touken/silhouette.py）——刮刮乐认刀的核心

**原理**：板子右侧剪影窗（固定坐标 `PANEL=(700,215,1020,460)`）里是纯黑剪影。
抠出来跟素材库 231 个模板（`*_head.png` / `*_foot.png` 两种视角）比轮廓：
双方轮廓线在各自 2px 容差带里的覆盖率取平均（"轮廓对称分"），FFT 互相关做全偏移搜索，5 个缩放比例（1.0~1.2）。
半分辨率（WORK_SCALE=0.5）下跑，单次 6~14 秒。

**已标定的阈值**（三块带标签板子验证：五虎退/小竜景光/浦島虎徹）：
- 真身得分 0.69~0.79，亚军 ≤0.43
- `CONFIDENT_SCORE=0.50`（第一名下限）、`CONFIDENT_MARGIN=0.08`（领先第二名下限）
- `MIN_BLACK_PIXELS=800`：格子翻太少（黑像素不够）直接拒答

**实测节奏**：一场战斗翻约 1 格，第 4~6 场时剪影够大，识别开始可靠。
流程里 `identify_min_battles=4` 之前不认，之后每场回来认一次，连续两次同名才动手。

**素材库格式**：`silhouette_lib/00047_五虎退_foot.png`（编号_名字_head|foot），300×300 白底黑影。
**如果 8 月新活动还是剪影玩法**：素材库直接复用；如果游戏内剪影位置/大小变了，只需重标定 `PANEL` 和 `DEFAULT_SCALES`（标定方法：打几局存 `board_after_bXX.png`，拿到真身后跑 `test_identify.py` 式的对照验证）。

---

## 9. 加新活动的标准流程（SOP，8 月 13 日照这个来）

1. **截模板图**（§4）：活动入口、界面标题 `ui活动名.png`、部队选择/即刻出阵/确认等按钮（大部分能复用 `team/` 目录里现有的）、结算/更新类按钮。目录在 `resource/base/image/` 下新建活动名文件夹。
2. **touken_config.json 加配置段**（照抄 `pumpkin` 段的结构改）：activity_entry、ui_title、deploy_button、team_ui_ocr、depart_button、confirm_ui/confirm_button、battle_ocr、round_end、skip_tap（安全区坐标一般不变）。
3. **flows/ 新建 `xxx.py`**：写 `XxxMixin`，主函数 `xxx_stream()` 生成器。导航用 `navigate_to_stream("出阵")` → 点活动入口 → 认 ui_title → 主循环（开战斗 → `battle_loop_stream` → 回来点安全区 → 完成判定 → 循环/收工）。
4. **flows/__init__.py + agent.py** 注册 Mixin（两处都要加，import 和类继承列表）。
5. **panel/server.py**：`_build_xxx` 函数 + `register_script("xxx", "显示名", "一句话简介", _build_xxx, params=[...])`；仪表盘文案在 `_SCRIPT_FLAVOR` 加一句骚话（如 `"pumpkin": "正在南瓜田里刨剪影🎃"`）。
6. **写 `test_xxx.py`**（照抄 test_pumpkin.py 的 MAA 初始化模板），先小步实测：1 局、上限调到最小。
7. **提醒用户重启面板**（她用 vbs 后台跑，后端改了不重启不生效）。

⚠️ 新活动第一天**先陪跑不托管**：用户在旁边看着，跑一遍死板版，把每个弹窗/动画/拒出阵情况都见过一遍，再放手。格子编队任务不满足会被游戏拒回部队选择界面——这种情况脚本要停下喊人改编队，不许自己瞎换队（编队策略太复杂，用户亲口说不做自动换队）。

---

## 10. 测试方法（很重要，Bash 有 300 秒上限）

- 每个 `test_*.py` 都是独立入口，`./.venv/Scripts/python.exe test_xxx.py` 直接跑。
- **长任务分多窗跑**：游戏状态在模拟器里持续，脚本支持断点续跑（重新导航回去接着来）。参考 `test_pumpkin_smart.py` 的"本窗口最多打 N 场"模式——打个 cap，超时前主动停，下一个窗口重跑继续。
- 调试数据采集：流程的 `debug_dir` 参数会存每一帧/每次回板子的截图，先采集再离线分析，别在线上瞎试。
- 改任何识别逻辑后，先跑离线验证（如 `test_identify.py` 用带标签数据），再上真机。

---

## 11. 血泪教训清单（别再犯）

1. 模拟器广告像素撞脸桌面图标模板 → 点图标前必须 OCR 担保（7-29 翻车，自动下载了别的游戏）。
2. 板子满没满靠猜 → 误烧更新令牌。只信官方「已解锁」文字。
3. RGBA 模板 + ROI = 匹配失灵。模板图一律 RGB。
4. 一刀解逻辑：锻刀收刀腾位置时顺手刀解过了，日课的刀解步骤要跳过（记录在历史里，别重复解）。
5. 面板"一键日课"冷启动后界面不稳时多点几次目录，等稳了再导航。
6. 用户打错过英文文件名（文盲 bug 原话）——看到路径里明显拼错的直接顺手修，不用问。

---

## 12. 用户协作风格（给下一个 AI）

- 中文大白话交流，她管助手叫 k3/宝宝。可以活泼，但**汇报必须实**：翻车了直说，烧了资源直说，不确定就说不确定。
- 她不懂代码但**流程逻辑极强**——游戏机制讲得比攻略还细。不懂玩法就问她，别瞎猜游戏机制。
- 她的截图素材都按分辨率裁好了放在 `resource/base/image/`，命名会告诉你用途。
- 额度有限（月额度 + 加油包），**干活要紧凑**：少废话，一次把活干完整，别来回试探。
- 她在意日志风格：喜欢有性格的（"正在南瓜田里刨剪影🎃"），不喜欢死板的。

---

## 13. 当前状态快照（2026-07-30）

- 南瓜大作战智能版：✅ 全链路验证通过（冷启动→认剪影→二次复核→烧令牌换板→继续）
- 令牌余额：用户买了 99 枚，测试烧了约 3 枚，活动 8 月 13 日结束
- 日课一条龙、联队战、演练、远征、内番、刷花：全部在面板可用
- 待办/可优化：silhouette_lab/（119MB 采集数据）和若干 test_*.py 可清理；README 是低调版（日服抓脚本，不大肆宣传，用户有意为之）
- 用户月额度 8 月 20 日重置，8 月 13 日新活动（疑似全新玩法）时需要照 §9 适配
