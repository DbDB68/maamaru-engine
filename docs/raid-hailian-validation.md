# 海边之阵接线验证（2026-09-24）

- 游戏画面取自 `MAAAdapter.screenshot(force=True)` 的 MuMu 1280×720 运行帧；完整帧只存用户数据目录的 `debug/k3_*_20260924.png`，不入库。
- 海联主页的 `lulian/ui海联.png` 在运行帧命中 1.000；陆联横幅未误命中。夜光贝累计 ROI 当前读到 `0`。
- 海联出阵确认标题与确定按钮分别在运行帧命中 1.000；旧陆联确认标题未误命中。当前三倍鱼笼持有数为 `0`，勾选模板未命中，流程应跳过道具。关闭确认窗后未耗通行令牌。
- `lulian/ui委托勾选.png` 从 `debug/k3_auto_after_delegate_20260924.png` 的 `(679,559,719,599)` 裁出。同一运行帧命中 1.000；未委托帧同 ROI 最高 0.393。真机完成「不委托→委托→回读勾选→不委托」恢复实验，代码也验证已委托时不重复点击。
- 三队有空刀装格；统一出阵链在真机识别警告并点击「整备刀装」，返回选队页，结果为未出阵。
- 2026-09-25 用四队、免费通行令牌实战完成海联超难一圈：自动行军已委托，出阵确认后回到海联主界面；游戏夜光贝从 `0` 到 `532`，记录 `raid.round_completed` 同圈 `shells_total=532`、`shells=532`。实图在用户数据目录 `debug/k3_raid_round_complete_20260925.png`，不入库。
- 战斗循环内部的 `battles` 只是「战斗」按钮命中次数，本次为 `22`，不等于真实战斗场数；展示与新记录改为只说完成一圈，并保留 `battle_taps` 供诊断。三倍鱼笼持有为 `0`，本圈明确关闭道具使用；补票关闭，未花小判。
- 本次用独立验收进程直接调用 Agent，因此事件没有面板任务编号；活动规划已读取到 `tama_current=532`、`tama_samples=1`，样本不足时保持剩余圈数未知。真机验收后重新打开自动行军窗，确认原本的「委托」勾选存在，再切回「不委托」并验到勾选消失。
