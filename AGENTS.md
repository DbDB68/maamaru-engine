# まあ丸仓库开发协作规范

这份文件只约束在本仓库中进行开发、修复、测试、打包和发布的 Agent。

如果用户的目标是**使用まあ丸完成游戏任务**，而不是修改まあ丸本身，请改读
[`docs/agent-user-guide.md`](docs/agent-user-guide.md)，并按其中的使用协助规范行动。
不要在开发对话中套用那份文档的初次接待话术。

## 开发协作

- 先确认用户要修改、诊断还是仅了解现状；诊断请求未包含修复授权时，不擅自改代码。
- 修改前检查现有实现、配置、测试和最近相关日志，避免让用户代替 Agent 排查。
- 保留工作区内与当前任务无关的改动；不要覆盖、回退或格式化用户正在进行的工作。
- 功能迁移或重构必须保持原有操作含义。尤其注意日课、演练、远征排班、连续出阵手入及活动虚拟伤害之间的差异。
- 涉及重伤判断、名单保护、资源消耗、自动更新和关机行为时，安全约束优先于便利性。
- 改动后进行与风险相称的验证；界面改动除构建外还应实际检查关键页面和响应式布局。
- 面向普通用户说明结果时使用游戏内叫法和生活化描述，先给结论，不直接倾倒 traceback、JSON 或长段日志。

## 前端维护

- 当前主前端位于 `panel/frontend/`，使用 Vue 3、TypeScript 和 Vite；`panel/static/` 中仍保留旧版前端和共用静态素材。
- 迁移功能时对照旧版行为与视觉，但不要为了复原样式破坏 Vue 组件的数据流或接口契约。
- 共用主题优先使用语义变量和可复用组件；主题可以改变色彩、纹理与有限布局性格，不得改变操作含义。
- 桌面端保证任务选择、当前任务、日志和本丸状态易读；窄屏应重排结构，避免强行缩放多列和制造嵌套滚动。
- 动效应服务于切换、运行、成功和失败反馈，并支持 `prefers-reduced-motion`。

## 发布与用户数据安全

- 新功能只能写入既有用户数据目录，不得把真实配置、日志、状态、调试文件或用户素材放回程序目录和发布包。
- 修改数据目录、数据结构、迁移、安装器或自动更新逻辑时，必须同时补充迁移或回滚测试；未经完整测试和隔离升级演练不得发布。
- 数据迁移必须先备份，只复制或按明确步骤转换，不得擅自删除来源；卸载与更新不得删除 `%LOCALAPPDATA%\Maamaru`。
- 自动更新只能接受まあ丸官方 GitHub Release 的安装器，下载后必须校验大小和 SHA-256；安装必须由用户明确确认，失败必须恢复旧程序。
- 不得为了实现新功能绕过上述规则。确需改变这些约束时，先向用户说明影响并取得明确同意。

## 长任务

- 对打包、下载、CI、批处理等后台长任务，不要高频轮询。
- 优先使用任务完成即返回的等待方式；允许较长等待时，通常以 180～300 秒为一次等待上限，再按实际任务调整。
- 长任务开始后简短说明正在做什么，并在持续工作期间保持适度进度反馈。
