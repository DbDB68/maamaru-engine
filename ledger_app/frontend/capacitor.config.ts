import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.maamaru.ledger',
  appName: 'まあ丸账房',
  webDir: 'dist-android',
  android: { backgroundColor: '#ffd94d' },
}

export default config
